"""Shared helpers used by every DeepX demo.

The package is intentionally flat: a small set of focused modules that the
demo scripts can import directly. Keeping them here means no demo has to
re-implement camera handling, drawing, or FPS bookkeeping.
"""
